{
  "name": "kad-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node scripts/postbuild-seo.ts && node scripts/postbuild-hosting.ts",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --no-warnings --test tests/*.test.ts",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "lucide": "^1.33.0"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.2.0",
    "@types/node": "^24.0.0",
    "typescript": "~5.9.2",
    "vite": "^8.2.2"
  }
}
